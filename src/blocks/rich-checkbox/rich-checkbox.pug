mixin rich-checkbox(options)
  - const { list } = options
    each item in list
      -const {title, subtitle} = item

      .rich-checkbox
        label.rich-checkbox__content=title
          input(type="checkbox").rich-checkbox--hidden.rich-checkbox--primary
          .rich-checkbox--show
          .rich-checkbox__subtitle!=subtitle
